class Solution:
    def elevatorRequests(self, n: int, requests: list[int]) -> int:
        curr=0
        time = 0
        for num in requests:
            time += abs(curr-num)
            curr = num
        return time
            
            
