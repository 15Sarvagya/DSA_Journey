class Solution:
    def smallestPalindrome(self, s: str) -> str:
        n = len(s)
        x = n//2
        left = sorted(s[:x])
        if n%2==0:
            return "".join(left)+ "".join(left[::-1])
        return "".join(left)+s[x]+ "".join(left[::-1])
