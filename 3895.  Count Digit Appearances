class Solution:
    def countDigitOccurrences(self, nums: list[int], digit: int) -> int:
        count =0
        for num in nums:
            x=num
            while(x):
                rem = x%10
                if rem==digit:
                    count+=1
                x=x//10
        return count
