class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
       j = set(jewels)
       count =0
       for s in stones:
        if s in j:
            count+=1
        
       return count
        
