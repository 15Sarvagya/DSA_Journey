# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
        #curr = head
        #count =0
        #while(curr!=None):
         #   count += 1
          #  curr = curr.next
        #n = count//2
        #curr=head
    
        #for i in range (n):
         #   curr = curr.next
        #return curr

        #New concept using fast and slow pointers
        slow = head
        fast= head
        while(fast!=None and fast.next!=None):
            slow= slow.next
            fast = fast.next.next
        return slow
