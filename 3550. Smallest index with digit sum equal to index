class Solution:
    def smallestIndex(self, nums: List[int]) -> int:
        for i,num in enumerate(nums):   
            Sum=0
            while(num):
                last=num%10
                Sum+=last
                num=num//10
            if Sum==i:
                return i
        return -1

